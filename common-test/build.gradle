dependencies {
    compile project(":common")
    compile project(":orders-history-common")

    compile "io.reactivex:rxjava:1.1.5"
    compile "junit:junit:4.12"

    compile 'io.eventuate.util:eventuate-util-test:0.1.0.RELEASE'

}